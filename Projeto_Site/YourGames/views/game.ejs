<!DOCTYPE html>
<html lang="pt-br">
  <% include partials/head %>
  
  <link rel="stylesheet" href="/stylesheets/game.css" />

  <body class="bg-dark-purple text-white">
    <% include partials/header %>

    <main class="container py-5">
      <div class="card bg-purple-dark border-0 shadow-lg p-4">
        <div class="row g-4 align-items-start"> 
          <!-- Capa -->
          <div class="col-md-4 text-center">
            <img src="<%= game.coverUrl %>" alt="<%= game.title %>" class="img-fluid rounded shadow-sm" />
          </div>

          <!-- Detalhes -->
          <div class="col-md-8">
            <h2 class="text-white mb-1"><%= game.title %></h2>
            <small class="text-light d-block mb-2">
            <%= game.year || 'Ano desconhecido' %> · Desenvolvido por <%= game.developer || 'Desconhecido' %>
            </small>
            <p class="text-light"><%= game.description %></p>
            <% if (game.genres.length > 0) { %>
              <div class="mb-3">
                <% game.genres.forEach(g => { %>
                  <span class="badge bg-purple text-white me-1"><%= g %></span>
                <% }) %>
              </div>
            <% } %>

            <div class="d-flex align-items-center gap-2">
              <span class="text-warning fs-5">⭐</span>
              <span class="text-white"><%= game.rating %> / 5</span>

            </div>
          </div>
        </div>
      </div>
      <!-- Comentários -->
        <section class="mt-5">
        <hr />
        <h5 class="mb-3">Opiniões da comunidade</h5>

        <div class="comment-box">
          <strong>@joaozin</strong>
          <p>Surpreendente, me fez chorar.</p>
        </div>

        <div class="comment-box">
          <strong>@gamer_lucas</strong>
          <p>Excelente narrativa e ambientação.</p>
        </div>
      </section>
    </main>
  </body>
</html>
